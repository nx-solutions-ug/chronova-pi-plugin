# Wiki Updated

Run completed at 2026-07-27T08:26:36.825Z.

## New pages

- `.wiki/.last-update-report.md`

  > I see the `.last-update-report.md` still contains old content. I should rewrite it cleanly to reflect this run.


## Updated pages

- `.wiki/development/ci-cd.md`

  > I found that the recent commit `0dd3638` added GitHub App token generation (`actions/create-github-app-token@v3`) to `vouch-pr.yml`, `vouch-manage.yml`, and `auto-manage.yml` — and the existing `.wiki/development/ci-cd.md` does not mention the `chronova-agent` GitHub App or the app-token pattern used across workflows. I will make a surgical edit to add that context.

- `.wiki/.last-updated.json`

  > I updated `.wiki/development/ci-cd.md` to document the GitHub App token pattern introduced by commit `0dd3638`. The new "GitHub App tokens" section explains how workflows use `actions/create-github-app-token@v3` with `APP_CLIENT_ID`/`APP_PRIVATE_KEY`, why `id-token: write` is declared, and which workflows use this pattern. I should also update the timestamp/metadata files so the wiki reflects the latest update.

- `.wiki/.last-update-title.txt`

  > Edited .wiki/.last-update-title.txt

- `.wiki/.last-update-report.md`

  > Edited .wiki/.last-update-report.md


## Summary

This update run created 1 page and edited 4 pages.
